{
  "name": "multisubtitle",
  "version": "0.1.0",
  "private": false,
  "description": "Chrome MV3 feasibility MVP for rendering a selectable subtitle overlay on Netflix playback pages.",
  "scripts": {
    "test": "node test/parser.test.js",
    "check": "node --check src/content.js && node --check src/popup.js && node --check src/page-hook.js && node --check src/background.js && node --check scripts/test-netflix.js && node test/parser.test.js",
    "test:netflix": "node scripts/test-netflix.js"
  },
  "devDependencies": {
    "playwright": "^1.45.0"
  },
  "license": "AGPL-3.0-or-later"
}
